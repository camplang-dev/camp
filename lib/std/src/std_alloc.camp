namespace Std;

public extern void* malloc(nuint size);
public extern void* realloc(void* ptr, nuint new_size);
public extern void free(void* ptr);

public interface Allocator
{
	void* alloc(nuint size);
	void* realloc(void* ptr, nuint newSize);
	void free(void* ptr);
}

public class HeapAllocator: Allocator
{
	public void* alloc(nuint size): Allocator
	{
		return malloc(size);
	}
	
	public void* realloc(void* ptr, nuint newSize): Allocator
	{
		return realloc(ptr, newSize);
	}
	
	public void free(void* ptr): Allocator
	{
		free(ptr);
	}
}
