using Std;

export void main()
{
	int[] list = [1, 2, 3];
	
	foreach (auto item in list.iterate())
	{
		Console.write(item);
	}
	
	Console.writeLine("DONE");
}

struct iter T iterate<T: copyable>(T[] this, sizeof(T))
{
	foreach (stackalloc T v in this)
		yield v;
}
