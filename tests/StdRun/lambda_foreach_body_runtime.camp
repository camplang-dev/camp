using Std;

export int main()
{
	int total = 0;
	int offset = 1;
	auto values = [1, 2, 3];

	foreach (int value in values)
	{
		delegate int() addOffset = () => value + offset;
		total = total + addOffset();
	}

	Console.writeLine(total);
	return total == 9 ? 0 : 1;
}
