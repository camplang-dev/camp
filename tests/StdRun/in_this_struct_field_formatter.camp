using Std;

struct MyValue
{
	int amount;
}

public nuint toString(in MyValue this, prep char[] buffer = default)
{
	return this.amount.toString(buffer);
}

export int main()
{
	MyValue value = { .amount = 1 };
	Console.writeLine($"item: {value}");
	return 0;
}
