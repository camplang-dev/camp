extern void* malloc(nuint size);
extern void free(void* ptr);
void consume(const char* value, nuint value_length)
{
}
char[] take(nuint length, out nuint result_length)
{
	char* value = (char*)malloc(sizeof(char) * length);
	nuint value_length = length;
	{
		result_length = value_length;
		return value;
	}
}
export int main()
{
	char* __local0 = default;
	bool __local1 = false;
	int __local2 = default;
	char* local;
	nuint local_length;
	local = take(3, out local_length);
	__local0 = local;
	__local1 = true;
	__local0;
	consume(local, local_length);
	char* __local3;
	nuint __local4;
	__local3 = take(4, out __local4);
	consume(__local3, __local4);
	{
		__local2 = 0;
		free((void*)__local3);
		if (__local1)
		{
			free((void*)__local0);
			__local1 = false;
		}
		return __local2;
	}
	free((void*)__local3);
	if (__local1)
	{
		free((void*)__local0);
		__local1 = false;
	}
}
