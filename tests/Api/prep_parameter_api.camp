namespace PrepApi;

export newtype fn nuint Formatter(prep char[] buffer = default);

export extern nuint formatValue(prep char[] buffer = default);

export interface Writer
{
	nuint getText(prep char[] buffer = default);
}

export extern class Thing
{
	export extern nuint getText(prep char[] buffer = default);
}
