struct iter byte chunks()
{
	fixed byte[4] scratch = [1, 2, 3, 4];
	yield scratch[0];
	scratch[0] = 5;
	yield scratch[1];
}

export int main()
{
	int total = 0;
	foreach (byte value in chunks())
	{
		total += (int)value;
	}
	return total;
}
