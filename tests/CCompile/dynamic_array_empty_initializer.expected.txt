// file: dynamic_array_empty_initializer.c
#include "dynamic_array_empty_initializer_private.h"


int main(void)
{
	uint32_t *_finally3 = 0;
	bool _cleanupActive4 = false;
	bool _cleanupActive6 = false;
	int _return7 = 0;
	uint32_t *values = (uint32_t *)(malloc((sizeof(uint32_t) * 4)));
	uintptr_t values_length = 4;
	uintptr_t _arrayInitIndex0 = 0;
	{
		while ((_arrayInitIndex0 < values_length))
		{
			values[_arrayInitIndex0] = 0;
			_arrayInitIndex0++;
__while_continue1: ;
		}
__while_break2: ;
	}
	_finally3 = values;
	_cleanupActive4 = true;
	_finally3;
	Pair *pair = (Pair *)(malloc((sizeof(Pair) * 2)));
	uintptr_t _arrayInitIndex5 = 0;
	while ((_arrayInitIndex5 < 2))
	{
		pair[_arrayInitIndex5] = (Pair){0};
		_arrayInitIndex5++;
	}
	_cleanupActive6 = true;
	values[0] = 10;
	pair[1].right = 20;
	{
		_return7 = (int)(((values[0] + pair[1].right)));
		if (_cleanupActive6)
		{
			free((void *)(pair));
			_cleanupActive6 = false;
		}
		if (_cleanupActive4)
		{
			free((void *)(_finally3));
			_cleanupActive4 = false;
		}
		return _return7;
	}
	if (_cleanupActive6)
	{
		free((void *)(pair));
		_cleanupActive6 = false;
	}
	if (_cleanupActive4)
	{
		free((void *)(_finally3));
		_cleanupActive4 = false;
	}
	return _return7;
}

// file: dynamic_array_empty_initializer.h
#ifndef DYNAMIC_ARRAY_EMPTY_INITIALIZER_H_
#define DYNAMIC_ARRAY_EMPTY_INITIALIZER_H_

#include "dynamic_array_empty_initializer_private.h"

void *malloc(uintptr_t size);
void free(void *ptr);
int main(void);

#endif
// file: dynamic_array_empty_initializer_private.h
#ifndef DYNAMIC_ARRAY_EMPTY_INITIALIZER_PRIVATE_H_
#define DYNAMIC_ARRAY_EMPTY_INITIALIZER_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct Pair Pair;

/* Enums. */

/* Newtypes. */

/* Layouts. */
struct Pair
{
	uint32_t left;
	uint32_t right;
};

/* Function declarations. */
void *malloc(uintptr_t size);
void free(void *ptr);
int main(void);

/* Object declarations. */


#endif
// compile
compiled: dynamic_array_empty_initializer.c
