extern void* malloc(nuint size);

int apply(escaped delegate int(int) callback, int value)
{
	return callback(value);
}

export int main()
{
	int offset = 40;
	return apply(new delegate value => value + offset, 2);
}
