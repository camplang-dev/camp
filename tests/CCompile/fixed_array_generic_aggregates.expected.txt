// file: fixed_array_generic_aggregates.c
#include "fixed_array_generic_aggregates_private.h"

/* Private file declarations. */
void *malloc(uintptr_t size);
static void PointerBox_set(PointerBox *this, void *value);
static void *PointerBox_getValue(const PointerBox *this);

static void PointerBox_set(PointerBox *this, void *value)
{
	this->value = value;
}

static void *PointerBox_getValue(const PointerBox *this)
{
	return this->value;
}

int main(void)
{
	uint8_t bytes[4] = {1, 2, 3, 4};
	uint8_t (*bytesPointer)[4] = &bytes;
	FixedValue fixedValue = (FixedValue){0};
	RefType *refValue = (RefType *)(malloc(sizeof(RefType)));
	if ((refValue != NULL))
	{
		*refValue = (RefType){0};
	}
	refValue->value = 13;
	PointerBox *byteBox = (PointerBox *)(malloc(sizeof(PointerBox)));
	if ((byteBox != NULL))
	{
		*byteBox = (PointerBox){0};
	}
	PointerBox_set(byteBox, bytesPointer);
	PointerBox *fixedBox = (PointerBox *)(malloc(sizeof(PointerBox)));
	if ((fixedBox != NULL))
	{
		*fixedBox = (PointerBox){0};
	}
	PointerBox_set(fixedBox, &fixedValue);
	PointerBox *refBox = (PointerBox *)(malloc(sizeof(PointerBox)));
	if ((refBox != NULL))
	{
		*refBox = (PointerBox){0};
	}
	PointerBox_set(refBox, refValue);
	InlineBuffer first = (InlineBuffer){0};
	__builtin_memcpy(first.data, (uint8_t[4]){5, 6, 7, 8}, sizeof(first.data));
	first.marker = 11;
	InlineBuffer second = first;
	return (((((int)(((uint8_t *)(bytes))[1]) + fixedValue.value) + ((RefType *)(refBox->value))->value) + (int)(((uint8_t *)(second.data))[2])) + second.marker);
}

// file: fixed_array_generic_aggregates.h
#ifndef FIXED_ARRAY_GENERIC_AGGREGATES_H_
#define FIXED_ARRAY_GENERIC_AGGREGATES_H_

#include "fixed_array_generic_aggregates_private.h"

int main(void);

#endif
// file: fixed_array_generic_aggregates_private.h
#ifndef FIXED_ARRAY_GENERIC_AGGREGATES_PRIVATE_H_
#define FIXED_ARRAY_GENERIC_AGGREGATES_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct RefType RefType;
typedef struct FixedValue FixedValue;
typedef struct InlineBuffer InlineBuffer;
typedef struct PointerBox PointerBox;

/* Enums. */

/* Newtypes. */

/* Layouts. */
struct FixedValue
{
	int value;
};
struct InlineBuffer
{
	uint8_t data[4];
	int marker;
};
struct RefType
{
	int value;
};
struct PointerBox
{
	void *value;
};

/* Function declarations. */
int main(void);

/* Object declarations. */


#endif
// compile
compiled: fixed_array_generic_aggregates.c
