// file: array_range_index.c
#include "array_range_index_private.h"

/* Private file declarations. */
static int main(void);

static int main(void)
{
	const char *message = "Hello, world!";
	uintptr_t message_length = 13;
	const char *firstWord = (message + ((0 > message_length) ? message_length : ((0 < 0) ? 0 : 0)));
	uintptr_t firstWord_length = ((((5 > message_length) ? message_length : ((5 < 0) ? 0 : 5)) >= ((0 > message_length) ? message_length : ((0 < 0) ? 0 : 0))) ? (((5 > message_length) ? message_length : ((5 < 0) ? 0 : 5)) - ((0 > message_length) ? message_length : ((0 < 0) ? 0 : 0))) : 0);
	const char *rest = (message + ((7 > message_length) ? message_length : ((7 < 0) ? 0 : 7)));
	uintptr_t rest_length = ((((message_length > message_length) ? message_length : ((message_length < 0) ? 0 : message_length)) >= ((7 > message_length) ? message_length : ((7 < 0) ? 0 : 7))) ? (((message_length > message_length) ? message_length : ((message_length < 0) ? 0 : message_length)) - ((7 > message_length) ? message_length : ((7 < 0) ? 0 : 7))) : 0);
	const char *middle = (message + ((1 > message_length) ? message_length : ((1 < 0) ? 0 : 1)));
	uintptr_t middle_length = (((((message_length - 1) > message_length) ? message_length : (((message_length - 1) < 0) ? 0 : (message_length - 1))) >= ((1 > message_length) ? message_length : ((1 < 0) ? 0 : 1))) ? ((((message_length - 1) > message_length) ? message_length : (((message_length - 1) < 0) ? 0 : (message_length - 1))) - ((1 > message_length) ? message_length : ((1 < 0) ? 0 : 1))) : 0);
	return (int)((((firstWord_length + rest_length) + middle_length)));
}

// file: array_range_index_private.h
#ifndef ARRAY_RANGE_INDEX_PRIVATE_H_
#define ARRAY_RANGE_INDEX_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */

/* Enums. */

/* Newtypes. */

/* Layouts. */

/* Function declarations. */

/* Object declarations. */


#endif
// compile
compiled: array_range_index.c
