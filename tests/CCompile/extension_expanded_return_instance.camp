const char[] toArray(string this)
{
	const char[] arr = {};
	arr.elements = this;
	arr.length = 0;
	return arr;
}

void accept(const char[] value)
{
}

int main()
{
	string value = "two";
	accept(value.toArray());
	accept(value.toArray());
	return 0;
}
