// file: static_type_fields.c
#include "static_type_fields_private.h"

/* Private file declarations. */
static int CounterValue;

const int Counter_START = 2;
int Counter_current = 0;
static int CounterValue = 3;
const int Holder_DEFAULT = 4;
const Id Id_INVALID = (Id)(0);
int main(void)
{
	Counter_current = ((Counter_START + Holder_DEFAULT) + CounterValue);
	Id id = Id_INVALID;
	return (Counter_current + (int)(id));
}

// file: static_type_fields.h
#ifndef STATIC_TYPE_FIELDS_H_
#define STATIC_TYPE_FIELDS_H_

#include "static_type_fields_private.h"

int main(void);
extern const int Counter_START;
extern const int Holder_DEFAULT;
extern const Id Id_INVALID;

#endif
// file: static_type_fields_private.h
#ifndef STATIC_TYPE_FIELDS_PRIVATE_H_
#define STATIC_TYPE_FIELDS_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct Counter Counter;
typedef struct Holder Holder;

/* Enums. */

/* Newtypes. */
typedef int Id;

/* Layouts. */
struct Holder
{
	int value;
};
struct Counter
{
};

/* Function declarations. */
int main(void);

/* Object declarations. */
extern const int Counter_START;
extern int Counter_current;
extern const int Holder_DEFAULT;
extern const Id Id_INVALID;


#endif
// compile
compiled: static_type_fields.c
