// file: retained_allocator_create_destroy.c
#include "retained_allocator_create_destroy_private.h"

/* Private file declarations. */
void *malloc(uintptr_t size);
void free(void *ptr);
static void *Allocator_alloc(Allocator *this, uintptr_t size);
static void Allocator_free(Allocator *this, void *ptr);
static void Inner_op_initnew(Inner *this);
static Inner *Inner_create(void);
static void Inner_op_delete(Inner *this);
static void Inner_destroy(Inner *this);
static void Retained_op_initnew(Retained *this, Allocator *allocator);
static Retained *Retained_create(Allocator *allocator);
static void Retained_op_delete(Retained *this);
static void Retained_destroy(Retained *this);
static int main(void);

static void *Allocator_alloc(Allocator *this, uintptr_t size)
{
	return malloc(size);
}

static void Allocator_free(Allocator *this, void *ptr)
{
	free(ptr);
}

static void Inner_op_initnew(Inner *this)
{
	this->value = 3;
}

static Inner *Inner_create(void)
{
	Inner *_created0 = (Inner *)(malloc(sizeof(Inner)));
	if ((_created0 != NULL))
	{
		*_created0 = (Inner){0};
		Inner_op_initnew(_created0);
	}
	return _created0;
}

static void Inner_op_delete(Inner *this)
{
	this->value = 0;
}

static void Inner_destroy(Inner *this)
{
	Inner_op_delete(this);
	free((void *)(this));
}

static void Retained_op_initnew(Retained *this, Allocator *allocator)
{
	this->allocator = allocator;
	Allocator *resolvedAllocator = allocator;
	this->value = 7;
	Inner *inner = (Inner *)(((resolvedAllocator != NULL) ? Allocator_alloc(resolvedAllocator, sizeof(Inner)) : malloc(sizeof(Inner))));
	if ((inner != NULL))
	{
		*inner = (Inner){0};
		Inner_op_initnew(inner);
	}
	(Inner_op_delete(inner), ((resolvedAllocator != NULL) ? Allocator_free(resolvedAllocator, (void *)(inner)) : free((void *)(inner))));
}

static Retained *Retained_create(Allocator *allocator)
{
	Allocator *resolvedAllocator = allocator;
	Retained *_created1 = (Retained *)(((resolvedAllocator != NULL) ? Allocator_alloc(resolvedAllocator, sizeof(Retained)) : malloc(sizeof(Retained))));
	if ((_created1 != NULL))
	{
		*_created1 = (Retained){0};
		Retained_op_initnew(_created1, allocator);
	}
	return _created1;
}

static void Retained_op_delete(Retained *this)
{
	this->value = 0;
}

static void Retained_destroy(Retained *this)
{
	Retained_op_delete(this);
	Allocator *resolvedAllocator = this->allocator;
	((resolvedAllocator != NULL) ? Allocator_free(resolvedAllocator, (void *)(this)) : free((void *)(this)));
}

static int main(void)
{
	Allocator *allocator = NULL;
	{
		Allocator *_allocator2 = allocator;
		{
			Retained *retained = Retained_create(_allocator2);
			Retained_destroy(retained);
		}
	}
	return 0;
}

// file: retained_allocator_create_destroy_private.h
#ifndef RETAINED_ALLOCATOR_CREATE_DESTROY_PRIVATE_H_
#define RETAINED_ALLOCATOR_CREATE_DESTROY_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct Allocator Allocator;
typedef struct Inner Inner;
typedef struct Retained Retained;

/* Enums. */

/* Newtypes. */

/* Layouts. */
struct Allocator
{
	char _camp_empty;
};
struct Inner
{
	int value;
};
struct Retained
{
	int value;
	Allocator *allocator;
};

/* Function declarations. */

/* Object declarations. */


#endif
// compile
compiled: retained_allocator_create_destroy.c
