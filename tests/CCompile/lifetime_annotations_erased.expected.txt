// file: lifetime_annotations_erased.c
#include "lifetime_annotations_erased_private.h"

/* Private file declarations. */
static void use(char *text, uintptr_t text_length, char *owner, uintptr_t owner_length);

static void use(char *text, uintptr_t text_length, char *owner, uintptr_t owner_length)
{
	char *scopedText = text;
	uintptr_t scopedText_length = text_length;
	char *anchoredText = text;
	uintptr_t anchoredText_length = text_length;
	char *escapedOwner = owner;
	uintptr_t escapedOwner_length = owner_length;
}

int main(void)
{
	char *value = 0;
	uintptr_t value_length = 0;
	return 0;
}

// file: lifetime_annotations_erased.h
#ifndef LIFETIME_ANNOTATIONS_ERASED_H_
#define LIFETIME_ANNOTATIONS_ERASED_H_

#include "lifetime_annotations_erased_private.h"

int main(void);

#endif
// file: lifetime_annotations_erased_private.h
#ifndef LIFETIME_ANNOTATIONS_ERASED_PRIVATE_H_
#define LIFETIME_ANNOTATIONS_ERASED_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */

/* Enums. */

/* Newtypes. */

/* Layouts. */

/* Function declarations. */
int main(void);

/* Object declarations. */


#endif
// compile
compiled: lifetime_annotations_erased.c
