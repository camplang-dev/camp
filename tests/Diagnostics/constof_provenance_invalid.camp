newtype IntPtr: const int*;

constof(source) int* firstElement(const int[] source)
{
	return source.elements;
}

void constToConstOf(const int[] source, const int* pointer)
{
	constof(source) int* bad = pointer;
}

void constOfToMutable(const int[] source)
{
	constof(source) int* dependent = firstElement(source);
	int* bad = dependent;
}

void differentAnchor(const int[] left, const int[] right)
{
	constof(left) int* dependent = firstElement(left);
	constof(right) int* bad = dependent;
}

void commonTypeIsOrdinaryConst(const int[] source, const int* pointer, bool condition)
{
	constof(source) int* dependent = firstElement(source);
	auto common = condition ? dependent : pointer;
	constof(source) int* bad = common;
}

void nominalBoundary(const int[] source)
{
	constof(source) int* dependent = firstElement(source);
	IntPtr bad = dependent;
}

export void main()
{
}
