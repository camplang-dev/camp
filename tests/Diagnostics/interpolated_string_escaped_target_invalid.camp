newtype delegate nuint CharFormatter(const this, char[] buffer = default);

nuint format(in int this, overload char[] buffer) : CharFormatter
{
	return 0;
}

escaped CharFormatter makeBad()
{
	int value = 1;
	return $"value {value}";
}
