struct Value
{
	int number;
}

void write(overload string value)
{
}

export void main()
{
	int count = 1;
	string text = "count " + count;
	text += count;

	Value value = { .number = 1 };
	write("value " + value);
}
