newtype delegate nuint CharFormatter(char[] buffer = default);

nuint formatBad(int this, overload char[] buffer) : CharFormatter
{
	return 0;
}

nuint formatGood(in int this, overload char[] buffer) : CharFormatter
{
	return 0;
}
