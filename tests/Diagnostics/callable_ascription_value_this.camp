newtype delegate nuint StringFormatter(char[] buffer = default);

nuint formatBad(int this, overload char[] buffer) : StringFormatter
{
	return 0;
}

nuint formatGood(in int this, overload char[] buffer) : StringFormatter
{
	return 0;
}
