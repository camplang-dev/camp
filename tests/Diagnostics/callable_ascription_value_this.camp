newtype delegate nuint TextFormatter(char[] buffer = default);

nuint formatBad(int this, overload char[] buffer) : TextFormatter
{
	return 0;
}

nuint formatGood(in int this, overload char[] buffer) : TextFormatter
{
	return 0;
}
