using Std;

export extern ByteWriter getWriter(out void* result_context);
export void writeSample(CharWriter writer, void* writer_context, thrown IoError error)
{
	CharWriter_writeLineCharArray(writer, writer_context, "sample", 6);
}
