using Std;

void main()
{
	HeapAllocator* allocator = HeapAllocator_create();
	void* memory = Allocator_alloc((Allocator*)allocator, 16);
	Allocator_free((Allocator*)allocator, memory);
	HeapAllocator_destroy(allocator);
}
