using Std;

void main()
{
	HeapAllocator* allocator = HeapAllocator_create();
	void* memory = HeapAllocator_alloc(allocator, 16);
	HeapAllocator_free(allocator, memory);
	HeapAllocator_destroy(allocator);
}
