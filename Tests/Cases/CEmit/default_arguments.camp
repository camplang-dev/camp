extern int externalDefault(int value = 7);

int addDefault(int left, int right = 5)
{
	return left + right;
}

export int run()
{
	int total = addDefault(3);
	total += externalDefault();
	return total;
}
