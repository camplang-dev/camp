using Std;

export int main()
{
	Console.writeCharArray("stream ");
	Console.writeInt(-12);
	Console.writeChar(' ');
	Console.writeUInt(34);
	Console.writeChar(' ');
	Console.writeBool(true);
	Console.newLine();
	return 0;
}
