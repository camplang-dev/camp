@symbol("SetWindowTextA")
extern bool SetWindowText(int hWnd, astring text);

@symbol("MyLibSomeValue")
export int SomeValue = 5;

@symbol("PrivateNumber")
int PrivateNumberSource = 2;

@symbol("privateImpl")
int helper()
{
	return 3;
}

class Control
{
	@symbol("ControlValue")
	export int getValue()
	{
		return 7;
	}

	@symbol("ComputeControlDefaultSize")
	export static int getDefaultSizeValue()
	{
		return 11;
	}
}

export int main()
{
	Control ctl = {};
	int val1 = ctl.getValue();
	int val2 = ctl.Value;
	int val3 = ControlValue(&ctl);
	int size1 = Control.getDefaultSizeValue();
	int size2 = Control.DefaultSizeValue;
	int size3 = ComputeControlDefaultSize();
	bool ok1 = SetWindowText(0, "a");
	bool ok2 = SetWindowTextA(0, "b");
	int some1 = SomeValue;
	int some2 = MyLibSomeValue;
	int hidden1 = PrivateNumberSource;
	int hidden2 = PrivateNumber;
	if (ok1 && ok2)
		return val1 + val2 + val3 + size1 + size2 + size3 + some1 + some2 + hidden1 + hidden2 + helper();
	return -1;
}
