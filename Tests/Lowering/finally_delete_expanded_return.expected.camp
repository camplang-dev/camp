extern void* malloc(nuint size);
extern void free(void* ptr);
void consume(const char* value, nuint value_length)
{
}
char* take(nuint length, out nuint result_length)
{
	char* result = (char*)malloc(sizeof(char) * length);
	nuint result_length = length;
	{
		result_length = result_length;
		return result;
	}
}
export int main()
{
	char* __local0 = default;
	bool __local1 = false;
	char* local;
	nuint local_length;
	local = take(3, out local_length);
	__local0 = local;
	__local1 = true;
	__local0;
	consume(local, local_length);
	char* __local2;
	nuint __local3;
	__local2 = take(4, out __local3);
	consume(__local2, __local3);
	int __local4 = default;
	{
		__local4 = 0;
		goto __cleanup4;
	}
	__cleanup4:
	free((void*)__local2);
	if (__local1)
	{
		free((void*)__local0);
		__local1 = false;
	}
	return __local4;
}
