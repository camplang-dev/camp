countIter count()
{
	return {.__state = 0};
}
int sum()
{
	int total = 0;
	{
		countIter __local0 = count();
		int __local1 = default;
		while (countIter_next(&__local0, &__local1))
		{
			int value = __local1;
			{
				total += value;
			}
			__foreach_continue2:
		}
		countIter_op_delete(&__local0);
		__foreach_break3:
	}
	return total;
}

fixed struct countIter
{
	int __state;

	bool next(int* current)
	{
		if (this.__state == -1)
			return false;
		if (this.__state == 1)
			goto __iter_resume1;
		*current = 1;
		this.__state = 1;
		return true;
		__iter_resume1:
		;
		this.__state = -1;
		return false;
	}

	void op_delete()
	{
		if (this.__state != -1)
		{
			this.__state = -1;
		}
	}

	void destroy()
	{
		countIter_op_delete(this);
	}
}
