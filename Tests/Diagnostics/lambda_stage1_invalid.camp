void usePredicate(fn bool(int) predicate)
{
}

void add(overload int value)
{
}

void add(overload fn bool(int) value)
{
}

void main()
{
	int threshold = 2;
	usePredicate(x => x > threshold);
	add(x => x > 2);
	delegate bool(int) delegated = x => x > 2;
}
