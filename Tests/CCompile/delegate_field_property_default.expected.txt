// file: delegate_field_property_default.c
#include "delegate_field_property_default_private.h"

/* Private file declarations. */
void *malloc(uintptr_t size);
void free(void *ptr);
static fn_void_voidPtr_ Window_getEventHandler(Window *this, void **result_context);
static void main_lambda0(void *context);

static fn_void_voidPtr_ Window_getEventHandler(Window *this, void **result_context)
{
	fn_void_voidPtr_ handler = this->windowState->eventHandler;
	void *handler_context = this->windowState->eventHandler_context;
	{
		(*result_context) = handler_context;
		return handler;
	}
}

int main(void)
{
	Window *window = (Window *)(malloc(sizeof(Window)));
	if ((window != NULL))
	{
		*window = (Window){0};
	}
	WindowState *_created0;
	_created0 = (WindowState *)(malloc(sizeof(WindowState)));
	if ((_created0 != NULL))
	{
		*_created0 = (WindowState){0};
	}
	window->windowState = _created0;
	{
		window->windowState->eventHandler = main_lambda0;
		window->windowState->eventHandler_context = NULL;
	}
	fn_void_voidPtr_ _call1;
	void *_context2;
	_call1 = Window_getEventHandler(window, &_context2);
	if ((_call1 == 0))
	{
		return 1;
	}
	return 0;
}

static void main_lambda0(void *context)
{
}

// file: delegate_field_property_default.h
#ifndef DELEGATE_FIELD_PROPERTY_DEFAULT_H_
#define DELEGATE_FIELD_PROPERTY_DEFAULT_H_

#include "delegate_field_property_default_private.h"

typedef void (* fn_void_voidPtr_)(void *arg0);
int main(void);

#endif
// file: delegate_field_property_default_private.h
#ifndef DELEGATE_FIELD_PROPERTY_DEFAULT_PRIVATE_H_
#define DELEGATE_FIELD_PROPERTY_DEFAULT_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct Window Window;
typedef struct WindowState WindowState;

/* Enums. */

/* Newtypes. */
typedef void (* WindowEventHandler)(void);

/* Callable typedefs. */
typedef void (* fn_void_voidPtr_)(void *arg0);

/* Layouts. */
struct Window
{
	WindowState *windowState;
};
struct WindowState
{
	fn_void_voidPtr_ eventHandler;
	void *eventHandler_context;
};

/* Function declarations. */
int main(void);

/* Object declarations. */


#endif
// compile
compiled: delegate_field_property_default.c
