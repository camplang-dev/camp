// file: lifetime_generic_boundaries_valid.c
#include "lifetime_generic_boundaries_valid_private.h"

/* Private file declarations. */
char *getEscapedPointer(void);
static void choose(void* value, void *result);
static int main(void);

static void choose(void* value, void *result)
{
	__builtin_memcpy(result, &(value), sizeof(void*));
}

static int main(void)
{
	char *source = getEscapedPointer();
	char *result = 0;
	choose(source, &result);
	int number = 0;
	choose((void *)(intptr_t)(7), &number);
	return number;
}

// file: lifetime_generic_boundaries_valid_private.h
#ifndef LIFETIME_GENERIC_BOUNDARIES_VALID_PRIVATE_H_
#define LIFETIME_GENERIC_BOUNDARIES_VALID_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */

/* Enums. */

/* Newtypes. */

/* Layouts. */

/* Function declarations. */

/* Object declarations. */


#endif
// compile
compiled: lifetime_generic_boundaries_valid.c
