// file: array_this_component_access.c
#include "array_this_component_access_private.h"

/* Private file declarations. */
static int IntArray_sum(const int* this, uintptr_t this_length);
static int main(void);

static int IntArray_sum(const int* this, uintptr_t this_length)
{
	int total = 0;
	{
		for (uintptr_t i = 0; (i < this_length); i++)
		{
			total += this[i];
__for_continue0:
		}
__for_break1:
	}
	return total;
}

static int main(void)
{
	int* values = (int []){1, 2, 3};
	uintptr_t values_length = 3;
	return ((IntArray_sum(values, values_length) == 6) ? 0 : 1);
}

// file: array_this_component_access_private.h
#ifndef ARRAY_THIS_COMPONENT_ACCESS_PRIVATE_H_
#define ARRAY_THIS_COMPONENT_ACCESS_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */

/* Newtypes. */

/* Enums. */

/* Layouts. */

/* Function declarations. */

/* Object declarations. */


#endif
// compile
compiled: array_this_component_access.c
