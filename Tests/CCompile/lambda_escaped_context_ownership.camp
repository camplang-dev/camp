extern escaped void* malloc(nuint size);
extern void free(escaped void* ptr);

escaped delegate int() makeCounter(int start)
{
	return new delegate () =>
	{
		int value = start;
		delete delegate;
		return value;
	};
}

export int main()
{
	auto callback = makeCounter(7);
	int value = callback();
	return 0;
}
