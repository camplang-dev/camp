// file: fixed_array_broad.c
#include "fixed_array_broad_private.h"

/* Private file declarations. */
static int sum(int *values, uintptr_t values_length);
static int GlobalValues[3];

static int GlobalValues[3] = {7, 8, 9};
static int sum(int *values, uintptr_t values_length)
{
	int total = 0;
	{
		for (uintptr_t i = 0; (i < values_length); i++)
		{
			total += values[i];
__for_continue0: ;
		}
__for_break1: ;
	}
	return total;
}

int main(void)
{
	int values[4] = {1, 2, 3, 4};
	int *span = (int *)(values);
	uintptr_t span_length = 4;
	int *ptr = values;
	int total = (((sum((int *)(values), 4) + sum(((int *)(values) + ((1 > 4) ? 4 : ((1 < 0) ? 0 : 1))), ((((3 > 4) ? 4 : ((3 < 0) ? 0 : 3)) >= ((1 > 4) ? 4 : ((1 < 0) ? 0 : 1))) ? (((3 > 4) ? 4 : ((3 < 0) ? 0 : 3)) - ((1 > 4) ? 4 : ((1 < 0) ? 0 : 1))) : 0))) + (int)(span_length)) + ptr[0]);
	Packet packet = (Packet){0};
	__builtin_memcpy(packet.data, (uint8_t[4]){1, 2, 3, 4}, sizeof(packet.data));
	uint8_t (*dataPointer)[4] = &packet.data;
	total += ((uint8_t *)(*dataPointer))[2];
	total += (int)(4);
	total += (int)(2);
	return (total + ((int *)(GlobalValues))[1]);
}

// file: fixed_array_broad.h
#ifndef FIXED_ARRAY_BROAD_H_
#define FIXED_ARRAY_BROAD_H_

#include "fixed_array_broad_private.h"

int main(void);

#endif
// file: fixed_array_broad_private.h
#ifndef FIXED_ARRAY_BROAD_PRIVATE_H_
#define FIXED_ARRAY_BROAD_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct Packet Packet;

/* Enums. */

/* Newtypes. */

/* Layouts. */
struct Packet
{
	uint8_t data[4];
	int matrix[2][3];
};

/* Function declarations. */
int main(void);

/* Object declarations. */


#endif
// compile
compiled: fixed_array_broad.c
