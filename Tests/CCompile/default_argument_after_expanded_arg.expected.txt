// file: default_argument_after_expanded_arg.c
#include "default_argument_after_expanded_arg_private.h"

/* Private file declarations. */
static int find(const char *value, uintptr_t value_length, bool flag);
static int main(void);

static int find(const char *value, uintptr_t value_length, bool flag)
{
	return (flag ? (int)(value_length) : 0);
}

static int main(void)
{
	const char *text = "hello";
	uintptr_t text_length = 5;
	return ((find(text, text_length, true) == 5) ? 0 : 1);
}

// file: default_argument_after_expanded_arg_private.h
#ifndef DEFAULT_ARGUMENT_AFTER_EXPANDED_ARG_PRIVATE_H_
#define DEFAULT_ARGUMENT_AFTER_EXPANDED_ARG_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */

/* Newtypes. */

/* Enums. */

/* Layouts. */

/* Function declarations. */

/* Object declarations. */


#endif
// compile
compiled: default_argument_after_expanded_arg.c
