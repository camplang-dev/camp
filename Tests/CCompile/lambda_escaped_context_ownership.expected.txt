// file: lambda_escaped_context_ownership.c
#include "lambda_escaped_context_ownership_private.h"

/* Private file declarations. */
void *malloc(uintptr_t size);
void free(void *ptr);
static int (* makeCounter(int start, void **result_context))(void *arg0);
static int makeCounter_lambda0(void *context);

static int (* makeCounter(int start, void **result_context))(void *arg0)
{
	makeCounter_lambdaContext0 *_lambdaContext0 = (makeCounter_lambdaContext0 *)(malloc(sizeof(makeCounter_lambdaContext0)));
	_lambdaContext0->capture0_start = start;
	{
		(*result_context) = _lambdaContext0;
		return makeCounter_lambda0;
	}
}

int main(void)
{
	int (* callback)(void *arg0);
	void *callback_context;
	callback = makeCounter(7, &callback_context);
	free((void *)(callback_context));
	return 0;
}

static int makeCounter_lambda0(void *context)
{
	makeCounter_lambdaContext0 *lambdaContext = (makeCounter_lambdaContext0 *)(context);
	return lambdaContext->capture0_start;
}

// file: lambda_escaped_context_ownership.h
#ifndef LAMBDA_ESCAPED_CONTEXT_OWNERSHIP_H_
#define LAMBDA_ESCAPED_CONTEXT_OWNERSHIP_H_

#include "lambda_escaped_context_ownership_private.h"

int main(void);

#endif
// file: lambda_escaped_context_ownership_private.h
#ifndef LAMBDA_ESCAPED_CONTEXT_OWNERSHIP_PRIVATE_H_
#define LAMBDA_ESCAPED_CONTEXT_OWNERSHIP_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct makeCounter_lambdaContext0 makeCounter_lambdaContext0;

/* Enums. */

/* Newtypes. */

/* Layouts. */
struct makeCounter_lambdaContext0
{
	int capture0_start;
};

/* Function declarations. */
int main(void);

/* Object declarations. */


#endif
// compile
compiled: lambda_escaped_context_ownership.c
