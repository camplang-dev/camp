// file: callable_this_ascription.c
#include "callable_this_ascription_private.h"

/* Private file declarations. */
void *malloc(uintptr_t size);
static uintptr_t Date_format(const Date *this, char *buffer, uintptr_t buffer_length);
static void Test_increment(Test *this);
static void Test_incrementExplicit(Test *this);
static void Test_normal(Test *this);

static uintptr_t Date_format(const Date *this, char *buffer, uintptr_t buffer_length)
{
	int y = this->year;
	return 0;
}

static void Test_increment(Test *this)
{
}

static void Test_incrementExplicit(Test *this)
{
}

static void Test_normal(Test *this)
{
}

int main(void)
{
	const Date d;
	fn_nuint_voidPtr__charPtr__nuint_ f = (uintptr_t (*)(void *arg0, char *arg1, uintptr_t arg2))Date_format;
	void *f_context = (void *)(&d);
	Test *test = (Test *)(malloc(sizeof(Test)));
	if ((test != NULL))
	{
		*test = (Test){0};
	}
	fn_void_voidPtr_ e1 = (void (*)(void *arg0))Test_increment;
	void *e1_context = (void *)((Test *)(test));
	fn_void_voidPtr_ e2 = (void (*)(void *arg0))Test_increment;
	void *e2_context = (void *)((Test *)(test));
	fn_void_voidPtr_ e3 = e2;
	void *e3_context = e2_context;
	fn_void_voidPtr_ e4 = (void (*)(void *arg0))Test_incrementExplicit;
	void *e4_context = (void *)((Test *)(test));
	fn_void_voidPtr_ n1 = (void (*)(void *arg0))Test_normal;
	void *n1_context = (void *)(test);
	fn_void_voidPtr_ n2 = (void (*)(void *arg0))Test_normal;
	void *n2_context = (void *)(test);
	fn_void_voidPtr_ n3 = n2;
	void *n3_context = n2_context;
	return 0;
}

// file: callable_this_ascription.h
#ifndef CALLABLE_THIS_ASCRIPTION_H_
#define CALLABLE_THIS_ASCRIPTION_H_

#include "callable_this_ascription_private.h"

typedef uintptr_t (* fn_nuint_voidPtr__charPtr__nuint_)(void *arg0, char *arg1, uintptr_t arg2);
typedef void (* fn_void_voidPtr_)(void *arg0);
int main(void);

#endif
// file: callable_this_ascription_private.h
#ifndef CALLABLE_THIS_ASCRIPTION_PRIVATE_H_
#define CALLABLE_THIS_ASCRIPTION_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct Date Date;
typedef struct Test Test;

/* Enums. */

/* Newtypes. */
typedef uintptr_t (* CharFormatter)(char *arg0, uintptr_t arg1);
typedef void (* EscapedDelegate)(void);
typedef void (* NormalDelegate)(void);

/* Callable typedefs. */
typedef uintptr_t (* fn_nuint_voidPtr__charPtr__nuint_)(void *arg0, char *arg1, uintptr_t arg2);
typedef void (* fn_void_voidPtr_)(void *arg0);

/* Layouts. */
struct Date
{
	int year;
};
struct Test
{
	char _camp_empty;
};

/* Function declarations. */
int main(void);

/* Object declarations. */


#endif
// compile
compiled: callable_this_ascription.c
