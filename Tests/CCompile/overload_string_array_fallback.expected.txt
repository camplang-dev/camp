// file: overload_string_array_fallback.c
#include "overload_string_array_fallback_private.h"

/* Private file declarations. */
void* malloc(uintptr_t size);
static void Writer_writeCharArray(Writer *this, const char* value, uintptr_t value_length);
static void Writer_writeInt(Writer *this, int value);
static void Writer_op_initnew(Writer *this);
static Writer* Writer_create(void);
static void writeCharArray(const char* value, uintptr_t value_length);
static void writeInt(int value);
static void run(Writer* writer);

static void Writer_writeCharArray(Writer *this, const char* value, uintptr_t value_length)
{
}

static void Writer_writeInt(Writer *this, int value)
{
}

static void Writer_op_initnew(Writer *this)
{
}

static Writer* Writer_create(void)
{
	Writer* _created0 = (Writer *)(malloc(sizeof(Writer)));
	if ((_created0 != NULL))
	{
		Writer_op_initnew(_created0);
	}
	return _created0;
}

static void writeCharArray(const char* value, uintptr_t value_length)
{
}

static void writeInt(int value)
{
}

static void run(Writer* writer)
{
	writeCharArray("asdf", 4);
	Writer_writeCharArray(writer, "qwer", 4);
}

// file: overload_string_array_fallback_private.h
#ifndef OVERLOAD_STRING_ARRAY_FALLBACK_PRIVATE_H_
#define OVERLOAD_STRING_ARRAY_FALLBACK_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct Writer Writer;

/* Newtypes. */

/* Enums. */

/* Layouts. */
struct Writer
{
	char _camp_empty;
};

/* Function declarations. */

/* Object declarations. */


#endif
// compile
compiled: overload_string_array_fallback.c
