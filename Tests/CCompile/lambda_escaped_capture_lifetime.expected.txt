// file: lambda_escaped_capture_lifetime.c
#include "lambda_escaped_capture_lifetime_private.h"

/* Private file declarations. */
static int __camp_delegate_main_lambda0(void *arg0);
void *malloc(uintptr_t size);
int *getEscapedInt(void);
static int apply(int (* callback)(void *arg0), void *callback_context);
static int main_lambda0(void *_context);

static int __camp_delegate_main_lambda0(void *arg0)
{
	return main_lambda0(arg0);
}

static int apply(int (* callback)(void *arg0), void *callback_context)
{
	return callback(callback_context);
}

int main(void)
{
	int *value = getEscapedInt();
	main_lambdaContext0 *_lambdaContext0 = (main_lambdaContext0 *)(malloc(sizeof(main_lambdaContext0)));
	_lambdaContext0->capture0_value = value;
	return apply(__camp_delegate_main_lambda0, _lambdaContext0);
}

static int main_lambda0(void *_context)
{
	main_lambdaContext0 *lambdaContext = (main_lambdaContext0 *)(_context);
	return *lambdaContext->capture0_value;
}

// file: lambda_escaped_capture_lifetime.h
#ifndef LAMBDA_ESCAPED_CAPTURE_LIFETIME_H_
#define LAMBDA_ESCAPED_CAPTURE_LIFETIME_H_

#include "lambda_escaped_capture_lifetime_private.h"

int main(void);

#endif
// file: lambda_escaped_capture_lifetime_private.h
#ifndef LAMBDA_ESCAPED_CAPTURE_LIFETIME_PRIVATE_H_
#define LAMBDA_ESCAPED_CAPTURE_LIFETIME_PRIVATE_H_

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

/* Forward declarations. */
typedef struct main_lambdaContext0 main_lambdaContext0;

/* Enums. */

/* Newtypes. */

/* Layouts. */
struct main_lambdaContext0
{
	int *capture0_value;
};

/* Function declarations. */
int main(void);

/* Object declarations. */


#endif
// compile
compiled: lambda_escaped_capture_lifetime.c
