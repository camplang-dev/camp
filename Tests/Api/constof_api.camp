export as ConstOfApi;

export struct Box<T>
{
	T value;
}

export class ConstOfHolder
{
	export ConstOfHolder()
	{
	}

	export constof(source) char* first(const char[] source)
	{
		return default;
	}

	export char* constof(source) suffixForm(const char[] source)
	{
		return default;
	}

	export Box<constof(source) char*> genericForm(const char[] source)
	{
		return default;
	}

	export constof(this) char* receiverFirst(const this)
	{
		return default;
	}
}

export newtype delegate constof(source) char* Picker(const char[] source);
