export as AsyncStage34;

export extern class Allocator
{
}

export extern class Resumer
{
	export extern void resumeAsync(escaped once void() continuation);
}
export extern async int loadAsync(within Allocator* escaped allocator);
